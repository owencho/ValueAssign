/*#include "AssignValue.h"
void assignValue(NameValueMap map[], char *str){
 
  
  
}

int isEqualSign(char *ptr){
  for (int i = 0; i<strlen(ptr);i++){
    if(ptr[i] == "="){
     return 1; 
    }
  }
  return 0;
}

char* NameFind(NameValueMap insert[],char *ptr){
  int size = sizeof(insert)/sizeof(insert[0]);
  for(i=0 , i<size ; i ++){
    
  
}
*/